.DS_store
Gemfile.lock
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
# Ignore folders generated by Bundler
.bundle/
vendor/

# Local copy of the remote_theme for reference; fetched at build time
hydejack/

# Editor / linter configs (per-developer, not part of the published site)
.vscode/
.markdownlint.json

# Project pages — moved to gh-pages branches of their own repos
# (donydchen/sem2nerf, donydchen/matchnerf, donydchen/mvsplat, donydchen/mvsplat360).
# Same /sem2nerf/, /matchnerf/, /mvsplat/, /mvsplat360/ URLs as before;
# they just don't live in this repo anymore.
sem2nerf/
matchnerf/
mvsplat/
mvsplat360/
